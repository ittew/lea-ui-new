import { expect } from 'chai'
// import { shallowMount } from '@vue/test-utils'
// import Button from '../../src/packages/Button'
// import Icon from '../../src/packages/Icon'

describe('Button.vue', () => { // 划分作用域
  it('1+1是否等于2', () => { // 三种情况相等  不相等  是否包含
    expect(1 + 1).to.eq(2)
  })
})
